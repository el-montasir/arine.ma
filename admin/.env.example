# Admin Panel (Vite) — REST API base URL for the ADMIN API.
# No secrets belong here; everything the browser can read is public by design.
VITE_API_URL="http://localhost:4000/api/admin"

# Optional: Public Storefront URL opened by the Admin topbar link (default: "/")
# VITE_STOREFRONT_URL="http://localhost:5173"